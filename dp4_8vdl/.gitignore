# Generated run artifacts — regenerable from 01_contigs/*.csv + scripts on the cluster.
02_rfd3/
04_mpnn/
05_af3/
06_metrics/
logs/

# Track the crystal and contig definitions (root .gitignore blanket-ignores *.pdb/*.csv, but this
# arm must be self-contained / pull-and-go).
!data/8VDL.pdb
!01_contigs/*.csv

# track committed figures
!figures/*.png
