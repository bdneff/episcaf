# results/

Small, **tracked** derived tables that figures and the manuscript depend on (shortlists,
summary stats). Each must be reproducible from a single cluster command — record that
command here next to the file it produces. Big raw data stays on `/tgen_labs` and is not
committed.

| file | produced by |
|------|-------------|
| _(add as you go)_ | _(the exact command)_ |
